spring:
  application:
    name:
      time-auction
  session:
    store-type: redis
  data:
    redis:
      host: localhost
      port: 6379
      # password: your_redis_password
  devtools:
    livereload:
      enabled: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8080

app:
  stomp:
    host: localhost
    port: 61613
    vhost: "/"
    user: ${RABBITMQ_USER:app}
    pass: ${RABBITMQ_PASS:app123!}

  
